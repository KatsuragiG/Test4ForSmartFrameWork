namespace TradeStops.Common.Enums
{
    // Do not forget to update EditSubtrade() method in PtSubtradesService if new types are added to this enum
    public enum PositionTransactionTypes : byte
    {
        Buy = 1,
        Sell = 2
    }
}
