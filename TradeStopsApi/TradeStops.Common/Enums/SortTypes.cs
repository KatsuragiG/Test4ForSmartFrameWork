namespace TradeStops.Common.Enums
{
    // Sorting direction. todo: replace with OrderTypes for consistency
    public enum SortTypes : byte
    {
        Ascending = 2,
        Descending = 4
    }
}
